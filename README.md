# Edzy Student Enrollment Form

A 4-step, multi-page student enrollment form built with Next.js (App Router), React Hook Form, and Zod. This project demonstrates complex form state management, cross-step validation, and persistent data storage.

## 🚀 Setup & Installation

1.  **Clone the repository:**
    ```bash
    git clone <your-repo-url>
    cd <your-repo-folder>
    ```

2.  **Install dependencies:**
    ```bash
    npm install
    ```

3.  **Initialize shadcn/ui (if not already done):**
    ```bash
    npx shadcn@latest init
    ```

4.  **Run the development server:**
    ```bash
    npm run dev
    ```

5.  Open [http://localhost:3000/enroll/step-1](http://localhost:3000/enroll/step-1) to start the form.

## 🛠️ Tech Stack

* **Framework:** Next.js 14+ (App Router)
* **Language:** JavaScript (ES6+)
* **Styling:** Tailwind CSS
* **UI Components:** shadcn/ui
* **Form Management:** react-hook-form
* **Validation:** zod & @hookform/resolvers

## 📂 Project Structure